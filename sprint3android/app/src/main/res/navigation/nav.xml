<?xml version="1.0" encoding="utf-8"?>
<!-- XML para la navegación entre fragmentos -->
<navigation xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:app="http://schemas.android.com/apk/res-auto"
    xmlns:tools="http://schemas.android.com/tools"
    android:id="@+id/nav"
    app:startDestination="@id/catalog">
    <!-- Definición de los fragmentos de navegación -->
    <!-- Fragmento para el Catálogo -->
    <fragment
        android:id="@+id/catalog"
        android:name="com.example.mycatalog.CatalogFragment"
        tools:layout="@layout/fragment_catalog" />
    <!-- Fragmento para Acerca de -->
    <fragment
        android:id="@+id/about"
        android:name="com.example.mycatalog.AboutFragment"
        tools:layout="@layout/fragment_about" />

</navigation>